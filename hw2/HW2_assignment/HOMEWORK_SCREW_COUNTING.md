# Lab2 作业：螺丝计数分析

## 1. 作业背景
在工业视觉品控环节中，螺丝的种类和数量检测是一项重要任务。由于不同类型的螺丝外观可能相似、光照条件多变且可能存在一定的遮挡等因素，使得计数准确性面临挑战。

本次作业要求大家针对螺丝图像进行分析。算法需要自动识别单张图像中的**5种**目标螺丝，并统计它们各自的数量。

---

## 2. 任务要求
你的算法需要接受一个包含多张测试图像的文件夹路径作为输入，对其中的每张图像进行目标检测与分类，并统计每种螺丝的数量（共 5 种）。

- **输入**：包含若干未知数量螺丝图像的文件夹。
- **输出**：一个 `.npy` 格式的文件，加载后为一个 Python 字典，格式详见下文。一个文本文件 `time.txt`，记录处理所有测试图像的总时间（单位：秒）。
- **核心逻辑**：你可以自行选择传统图像处理方法或深度学习方法，不限制具体算法。
- **数据说明**：大家可以利用**第一次作业中矫正后的俯视图像**作为本次作业的训练集或验证集（需要自行标注）。

---

## 3. 数据集说明

我们需要检测和计数的螺丝如下图分为 5 种，后续将按照固定的种类序号 `[Type_1, Type_2, Type_3, Type_4, Type_5]` 记录数量。

![螺丝示例图](./screws.png)

### 3.1 提供的开发数据
为了方便大家理解任务并验证代码逻辑，我们提供了：
- **2 张示例图像**
- **参考输出样例**（与这 2 张图像对应的真实螺丝数量及格式样例）

- 示例1:
![示例图像1](./01.png)
- 示例2:
![示例图像2](./02.png)

- 参考输出样例：
```python
out_dict ={
    '示例1': [6, 3, 4, 2, 4],
    '示例2': [14, 1, 1, 8, 1]
}
```


### 3.2 评分测试数据
评分时，我们将使用一个提前准备好的包含 **20张图像** 的测试集，这些图像的场景光照可能和示例数据有一定的区别，但都是俯视角。

---

## 4. 提交内容及格式要求（必须遵循）

本次作业分为**两部分独立提交**：
1. **实验报告**：不需要放入压缩包中，直接提交到 **Canvas** 系统即可。
2. **代码压缩包**：其余代码等文件须上传至**交大云盘**（提交链接如下）。请将你的提交目录命名为 `submission`并打包为 `.zip` 格式（保持该命名规范），上传到交大云盘会自动补充你的学号。

**提交链接：**
```
https://pan.sjtu.edu.cn/web/collect/bf495b9b978ba9237606bd5c18f51157
```

**交大云盘提交代码压缩包的目录结构要求**：

```text
submission.zip/
  code/
    run.py           # 核心运行接口
    requirements.txt # (可选但推荐) 依赖包列表
    README.md        # 必须：说明环境配置及运行方式
    ...              # 其他代码或模型权重文件
```




### 4.1 程序入口 (`run.py` & `.npy` 输出)
为了便于统一自动化阅卷，请确保你的代码可以通过如下命令行格式一键运行：
```bash
python run.py --data_dir /path/to/test_images --output_path ./result.npy --output_time_path ./time.txt
```
尽量用灵活的导入路径，确保在任何环境下都能正确执行。

输出的 `result.npy` 文件通过 `numpy.load(..., allow_pickle=True).item()` 加载后，**必须是一个字典**。
- **Key**：图像文件名（**不要包含后缀**，例如图像为 `img_001.jpg`，则键为 `'img_001'`）
- **Value**：一个长度为 5 的 一维列表/NumPy 向量（代表按 1-5 种类型顺序的螺丝个数）

字典样例：
```python
{
    'img_001': [2, 0, 1, 4, 0],
    'img_002': [1, 1, 0, 5, 2]
}
```

输出的 `time.txt` 文件应包含一个数字，代表处理所有测试图像的总时间（单位：秒），便于我们评估速度。

### 4.2 环境配置说明 (`README.md`)
在代码目录下，必须提供清晰的 `README.md`，包含：
- Python 版本及系统要求。
- 必要的第三方库安装命令（如有 `requirements.txt` 请注明）。
- 若使用深度学习模型，请说明预训练权重的相对路径（如需要）。

### 4.3 实验报告 (`report.pdf`，提交至 Canvas)
要求包含：
- **算法设计与流程**：你采用了什么方法（传统机器视觉还是深度学习）？网络结构或处理流程是怎样的？
- **实验与调优细节**：训练过程（若有数据增强、超参调试）或形态学参数怎么选定的。
- **结果分析与局限性**：在样例图像或你的验证集上表现如何？算法还存在哪些容易失败的情况？

---

## 5. 评分标准

本次作业满分为 100 分。各部分得分占比如下：

### 5.1 结果得分 (60%)
结果部分总分为 60 分，其中包含 50 分的**计数准确度得分**与 10 分的**运行速度得分**。

为了使评分更加透明且容错，对于20张盲测图像中的每张图像，我们将分别统计这5种螺丝的预测数量。一共有 $20 \times 5 = 100$ 个计数值需要预测。在 50 分的计数准确度满分标准下，每个独立计数值满分为 **0.5 分**。

对于第 $i$ 张图的第 $j$ 类螺丝，设真实数为 $y_{i,j}$，你的预测数为 $\hat{y}_{i,j}$，计数的绝对误差为 $e_{i,j} = | \hat{y}_{i,j} - y_{i,j} |$。我们将根据误差值给予不同梯度的分数：

- **完全准确**（$e_{i,j} = 0$）：得满分 **0.5 分**。
- **误差为 1**（$e_{i,j} = 1$，即多检或漏检 1 个）：得部分分数 **0.25 分**。
- **误差大于等于 2**（$e_{i,j} \ge 2$）：该项得 **0 分**。

最后的结果总得分将是这 100 个计数值得分累加的总和：
$$ \text{结果得分} = \sum_{i=1}^{20} \sum_{j=1}^{5} \text{Score}(e_{i,j}) $$
满分为50分。

**特别提醒**：
- 即使某一类螺丝表现不佳，只要其余类别或其余图像能够正确计数，依然能积累拿到对应的分数。
- 只有正常运行输出符合格式的字典才能进入自动阅卷。若代码崩溃、未能生成 `.npy` 文件或文件格式错误导致解析失败，此部分得分直接记为 0。

#### 速度评分（10分）

- 代码速度也在评分考虑范围内：
    - 我们会读取 `time.txt` 中记录的总处理时间来评估速度表现，会同时查看代码实现细节，确保没有明显的作弊行为（如直接输出固定结果而不处理图像）。
    - 在标准测试环境下，每张图像处理时间不超过 5 秒为满分 10 分，也就是总时间在 100 秒以内。
    - 总时间每超过60秒扣除 2 分；
    - 请在代码最终的输出里附加一个总处理时间的打印，便于我们灵活评估速度。

- 测试硬件环境：

```
Architecture:                    x86_64
CPU op-mode(s):                  32-bit, 64-bit
Byte Order:                      Little Endian
Address sizes:                   46 bits physical, 48 bits virtual
CPU(s):                          40
On-line CPU(s) list:             0-39
Thread(s) per core:              2
Core(s) per socket:              10
Socket(s):                       2
NUMA node(s):                    2
Vendor ID:                       GenuineIntel
CPU family:                      6
Model:                           85
Model name:                      Intel(R) Xeon(R) Silver 4210 CPU @ 2.20GHz
Stepping:                        7
CPU MHz:                         1624.845
CPU max MHz:                     3200.0000
CPU min MHz:                     1000.0000
BogoMIPS:                        4400.00
Virtualization:                  VT-x
L1d cache:                       640 KiB
L1i cache:                       640 KiB
L2 cache:                        20 MiB
L3 cache:                        27.5 MiB
GPU model:                       NVIDIA 2080 Ti
GPU memory:                      11 GB
CUDA version:                    11.8
```




### 5.2 报告得分 (25%)
- 逻辑清晰，算法表述准确完整。
- 客观的成功与失败原因分析。
- 格式规范，图文并茂者佳。

### 5.3 代码规范与可运行性 (15%)
- 严格遵循 `run.py` 的接口规范和参数说明。
- 附带了清晰的环境配置说明（如 `README.md` 与 `requirements.txt`）。
- 代码具有良好的模块化结构与注释，便于助教检查算法的各个部分实现细节，并顺利跑通环境。

---

## 6. 提交前检查清单
- [ ] 代码是否包含 `run.py`，且启动接口完整支持 `--data_dir`、`--output_path` 与 `--output_time_path` 参数？
- [ ] 导出的 `npy` 字典 key 是否是不带后缀的图像名？value 是否严格为 5x1 的列表/向量？
- [ ] 是否成功输出包含总处理时间的 `time.txt`，并在终端打印了该时间值？
- [ ] 是否在 `code/` 内部提供了包含环境说明的 `README.md`？
- [ ] 是否已将撰写完成的 `report.pdf` 单独提交到了 **Canvas** 系统？
- [ ] 是否已将代码目录规范命名为 `submission` 并压缩为了 `zip` 文件，成功上传至**交大云盘**？