# 📋 实验执行清单
根据重新组织的幻灯片大纲，你们团队需要执行以下3 个定量实验：

## 实验 A: 推理密度对精度/速度的影响
目标：评估 --infer_stride 参数对计数精度与运行速度的权衡

执行步骤：
```python
# 配置 1：全帧推理（基线）
python core/tracker.py --source ./test_videos/IMG_2376.MOV --weights ./weights/best.pt --infer_fps 30

# 配置 2：2 倍跳帧
python core/tracker.py --source ./test_videos/IMG_2376.MOV --weights ./weights/best.pt --infer_stride 2

# 配置 3：4 倍跳帧  
python core/tracker.py --source ./test_videos/IMG_2376.MOV --weights ./weights/best.pt --infer_stride 4
```
记录指标：

处理时间（秒）→ 计算 FPS

每类计数结果（与基线对比）

虚假重复数（从控制台按类统计）

输出：表格 + 曲线图（FPS vs 误差）

## 实验 B: 三级去重各层的边际贡献
目标：量化 L1、L2、L3 各去重层的有效性

执行方式：
在 video_tracker.py 中修改去重逻辑，创建 4 个版本：

基线（三层都开）：正常运行

关闭 L1（本地去重）：注释掉 IoU > 0.15 检查

关闭 L2（地图去重）：注释掉 map_center_ratio 匹配

关闭 L3（分裂处理）：注释掉 split 启发式规则

对同一视频运行 4 次，对比结果：

虚假重复数量增加多少

总计数误差变化

输出：表格（每层开关 vs 虚假重复数）+ 柱状图

## 实验 C: 边界 ROI 效应与参数敏感性
目标：评估边界附近物体的检测与计数稳定性

执行方式：

修改 video_tracker.py 中的 roi_margin 参数，分别指定为：

额外分析：

关闭 ROI 衰减（设为 0），记录虚假计数增加量

针对进出边界物体多的视频特别对比

输出：表格（ROI margin vs 计数数组）+ 定性说明